import styled from 'styled-components';

export const App = () => (
  <header>
    <Heading>COUNTRY QUIZ</Heading>
  </header>
);

const Heading = styled.h1`
  font-size: 2.25rem;
  font-weight: 700;
  color: #fff;
`;
